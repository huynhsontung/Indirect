<UserControl
    x:Class="Indirect.Controls.ReelProgressIndicator"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Indirect.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:converters="using:Indirect.Converters"
    xmlns:system="using:System"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <UserControl.Resources>
        <ResourceDictionary>
            <converters:DoubleFromTupleConverter x:Key="DoubleFromTupleConverter"/>
            <converters:BrushFromTupleConverter x:Key="BrushFromTupleConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <muxc:ItemsRepeater ItemsSource="{x:Bind _indicatorCollection}">
        <muxc:ItemsRepeater.Layout>
            <muxc:StackLayout Orientation="Horizontal" Spacing="4"/>
        </muxc:ItemsRepeater.Layout>
        <DataTemplate>
            <!--<ProgressBar Value="100" Width="100"/>-->
            <Rectangle Width="100" Height="5" Fill="White"></Rectangle>
        </DataTemplate>
    </muxc:ItemsRepeater>
</UserControl>
