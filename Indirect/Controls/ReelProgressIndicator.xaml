<UserControl
    x:Class="Indirect.Controls.ReelProgressIndicator"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Indirect.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:utilities="using:Indirect.Utilities"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    SizeChanged="ReelProgressIndicator_OnSizeChanged">

    <muxc:ItemsRepeaterScrollHost>
        <ScrollViewer HorizontalScrollMode="Disabled" VerticalScrollMode="Disabled" ZoomMode="Disabled" Height="4" MaxHeight="4">
            <muxc:ItemsRepeater ItemsSource="{x:Bind _indicatorCollection}" Height="4">
                <muxc:ItemsRepeater.Layout>
                    <muxc:StackLayout Orientation="Horizontal" Spacing="2"/>
                </muxc:ItemsRepeater.Layout>
                <DataTemplate x:DataType="utilities:ProgressItem">
                    <ProgressBar Value="{x:Bind Value, Mode=OneWay}" Width="{x:Bind Width, Mode=OneWay}" Height="4"/>
                </DataTemplate>
            </muxc:ItemsRepeater>
        </ScrollViewer>
    </muxc:ItemsRepeaterScrollHost>
</UserControl>
