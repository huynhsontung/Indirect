<Page
    x:Class="InstantMessaging.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:InstantMessaging"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:InstaSharper.Classes.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition MinWidth="90"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="90"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <ListView x:Name="InboxList" Grid.Row="1" ItemsSource="{x:Bind ViewModel.InboxThreads, Mode=OneWay}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:InstaDirectInboxThread">
                        <StackPanel x:Name="InboxItem" Orientation="Horizontal" Margin="6">
                            <SymbolIcon Symbol="Contact" Margin="0,0,12,0"/>
                            <StackPanel>
                                <TextBlock Text="{x:Bind Users[0].UserName}" FontWeight="Bold"/>
                                <TextBlock Text="{x:Bind Items[0].Text}"/>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </Grid>
    </Grid>
</Page>
